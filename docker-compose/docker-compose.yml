services:
  pgassistant:
    image: test:0
    restart: always
    environment:
      - OPENAI_API_KEY=mykey
    ports:
      - "8080:5005"
    volumes:
      - ./myqueries.json:/home/pgassistant/myqueries.json